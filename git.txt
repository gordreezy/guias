# Para usuario root
$ para usuario regular

----

# aptitude install git // Instalación del paquete git

$ git config --global user.name "Mi nombre y apellido" // Asignamos el nombre de usuario que será la firma del usuario en cada commit.

$ git config --global user.email miemail@example.com // igualmente el correo que siempre acompañara al autor de cada commit.

$ git init // Inicializar una carpeta como repositorio de git.

$ git status // Ver si hay cambios en el repositorio

$ git log // Ver los commits del repositorio

$ git add * // Agregar todos los cambios al seguimiento de git

$ git commit -m "descripcion del commit" // Hacer un commit

$ git push origin master // Subir los cambios al repoisitorio en la nube, nos pedira el nombre de usuario y contraseña de la cuenta de github

$ git diff // Ver los cambios realizados
